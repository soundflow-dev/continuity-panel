# Agentic OS defaults

Application projects are stored under the Agentic OS `projects/` directory.

When entering a project, read its `AGENTS.md` and `PROJECT_STATE.md` before making changes. Update `PROJECT_STATE.md` before handing work to another agent. Never store credentials in shared memory or repository files.

